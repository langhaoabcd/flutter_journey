import '../utils/net/http.dart';

class AccountService {
  // Future<PageModel<ArticleModel>> articlePage(int page) async {
  //   var res = await HttpManager.get("article/list/$page/json", {});
  //   return toArticlePage(res);
  // }

  // PageModel<ArticleModel> toArticlePage(Map<String, dynamic> res) {
  //   var data = res["data"];
  //   var list = data["datas"]
  //       .map<ArticleModel>((item) => ArticleModel.fromJson(item))
  //       .toList();
  //   return PageModel(data["curPage"], list, data["offset"], data["over"],
  //       data["pageCount"], data["size"], data["total"]);
  // }
}
